extends layout

block content
  div(id="landing" class="blue")
    div(class="row")
      h1(class="centered") Sample Service Status
      hr(class="partial")
      p(class="partial") The Sample  service manages samples, neurons, and related information such as injections, registration transformation types, and viruses.  This status service is a diagnostic element of the NDB system and is generally not directly accessed by users.
      p(class="partial") Use this site to quickly view the status of the service.
      br
      div(class="centered")
        p(class="lead")
        p Status server is #[span(id="server_status") down]
        p API server is #[span(id="service_status") down]
        p Database is #[span(id="db_status") unavailable]
        br
        h4 #[span(id="neuron_count") ?] neurons in #[span(id="sample_count") ?] samples
        br
      div(class="table-counts")
        table(class="table table-sm table-bordered")
          tr
            td Registration Transforms
            td #[span(id="registration_transform_count") ?]
          tr
            td Mouse Strains
            td #[span(id="mouse_strain_count") ?]
          tr
            td Injections
            td #[span(id="injection_count") ?]
          tr
            td Injection Viruses
            td #[span(id="injection_virus_count") ?]
          tr
            td Fluorophores
            td #[span(id="fluorophore_count") ?]
          tr
            td Brain Areas
            td #[span(id="brain_area_count") ?]
  script(src='/lib/io.js')